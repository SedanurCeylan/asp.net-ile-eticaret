@model Urun

<div class="card shadow">
    <div class="img-wrap position-relative pt-2 ps-2">
        <span class="badge bg-success position-absolute">İndirim</span>
        <img src="~/img/@Model.Resim" alt="@Model.UrunAdi" class="card-img-top">
    </div>
    <div class="border-top info-wrap p-3">
        <a href="#" class="float-end btn btn-light">
            <i class="fa-regular fa-heart"></i>
        </a>
        <a asp-controller="Urun" asp-action="Details" asp-action-itemid="@Model.Id" class="title text-truncate d-block pe-2 text-decoration-none text-secondary-emphasis ">@Model.UrunAdi</a>
        <div class="price-wrap">
            <span class="price-discount">@Model.Fiyat</span>
            @* <del class="price">49.4999tl</del> *@
        </div>
    </div>
</div>