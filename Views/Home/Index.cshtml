@model List<Urun>

    <section class="mt-3">
        <div class="container">
            <div class="card shadow">
                <div class="row gx-2 p-1 p-lg-2">
                <div class="col-lg-3 d-none d-lg-block">
                   @await Html.PartialAsync("Partials/_Menü")
                </div>
                <div class="col-lg-9">
                     @await Html.PartialAsync("Partials/_Slider")
                </div>
                </div>
            </div>
        </div>
    </section>

    <section class="mt-3">
        <div class="container">
            <h3 class="h4 mb-1 mb-lg-3">Son Eklenenler</h3>
            <div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 g-3">
                <div class="col">
                    @await Html.PartialAsync("Partials/_UrunCard")
                </div>
                
            </div>
        </div>

    </section>